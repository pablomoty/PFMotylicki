import { MensajeErrorPipe } from './mensaje-error.pipe';

describe('MensajeErrorPipe', () => {
  it('create an instance', () => {
    const pipe = new MensajeErrorPipe();
    expect(pipe).toBeTruthy();
  });
});
